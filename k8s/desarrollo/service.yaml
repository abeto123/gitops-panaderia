apiVersion: v1
kind: Service
metadata:
  name: api-servicey
  labels:
    app: hexagonal-api
spec:
  type: NodePort
  selector:
    app: hexagonal-api
  ports:
  - name: http
    protocol: TCP
    port: 80
    targetPort: 8080
    nodePort: 30200
